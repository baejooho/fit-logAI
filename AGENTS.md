# AGENTS.md — AI 에이전트 활용 정책 및 워크플로우

본 문서는 FitLog AI 프로젝트 개발 과정에서 AI 에이전트(Claude Code)를
활용한 워크플로우와 코딩 정책을 기록한다.

## 1. 사용한 AI 에이전트

- **Claude Code** (터미널 기반 AI 코딩 에이전트): 프로젝트 구조 생성,
  코드 작성/수정, 패키지 설치, 테스트 작성, 디버깅에 활용
- **Claude (대화형)**: 발표 자료 기획, 문서(README, ADR 등) 작성,
  설계 방향 의사결정 보조에 활용

## 2. AI 에이전트에게 위임한 작업

| 작업 | 위임 여부 | 비고 |
|------|-----------|------|
| 프로젝트 초기 구조 생성 (package.json, tsconfig.json) | 위임 | Claude Code가 생성 |
| Gemini API 연동 코드 작성 | 위임 | 모델명 변경(404→2.5-flash) 등 디버깅 포함 |
| Express 웹 폼 및 결과 화면 구현 | 위임 | 요일별 탭, 이미지 매핑, 수행 무게 입력 포함 |
| routines.json 저장/조회/삭제 로직 | 위임 | 최대 20개 제한, 다음 주 루틴 생성 로직 포함 |
| 점진적 과부하 프롬프트 설계 | 협업 | 방향(수행 무게 기록 → 다음 루틴 반영)은 직접, 세부 문구는 AI 작성 |
| 버그 수정 (모델 404, 다음 주 설정 미반영, 추가요구사항 미반영 등) | 위임 | 에러/현상을 AI에게 전달하여 반복 수정 |
| 단위/통합 테스트 작성 (Jest, supertest) | 위임 | db.test.ts, web.test.ts 작성 및 PASS 확인 |
| 문서화 (README, PLANNING, WBS, ARCHITECTURE, ADR, SETUP) | 위임 | 프로젝트 정보를 기반으로 AI가 작성 |

## 3. 개발 워크플로우

1. 기능 요구사항을 자연어로 AI 에이전트에게 전달
2. AI가 코드를 생성/수정
3. `npm run web`으로 직접 실행하여 동작 확인
4. 에러/이상 동작 발생 시 그대로 AI에게 전달하여 반복적으로 수정
5. `npm test`로 자동화 테스트 통과 확인
6. 동작이 확인되면 Git에 커밋 및 push

## 4. 코딩 정책

- **언어**: TypeScript 사용 (타입 안정성 확보, types.ts에 공통 타입 정의)
- **민감 정보 관리**: API 키 등은 `.env`로 분리, `.gitignore`에 포함
  (`.env.example`로 필요한 변수 안내)
- **AI 모델 교체 용이성**: AI 호출 로직(claude.ts)을 별도 모듈로 분리하여,
  추후 다른 LLM API로 교체 시 영향 범위를 최소화
- **저장소 추상화**: 저장 로직(db.ts)을 별도 모듈로 분리하여, JSON 파일
  → SQLite 전환 시 다른 모듈에 영향 최소화
- **점진적 개발**: 4주 플랜 → 1주 루틴+피드백 루프, SQLite → JSON 파일
  등 범위를 1인 프로젝트에 맞게 조정하며 핵심 기능의 완성도를 우선
  (자세한 배경은 [docs/ADR.md](docs/ADR.md) 참고)

## 5. 향후 AI 활용 개선 방향

- 단위/통합 테스트 커버리지 확대에 AI 에이전트 활용
- 코드 리뷰 단계에서 AI 기반 정적 분석 도입 검토